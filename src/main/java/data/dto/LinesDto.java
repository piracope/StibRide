package data.dto;

public class LinesDto extends Dto<Integer> {

    /**
     * Creates a new instance of <code>LinesDto</code> with the key of the data.
     * <p>
     * The key is the line's number.
     *
     * @param key key of the data.
     */
    public LinesDto(Integer key) {
        super(key);
    }
}
